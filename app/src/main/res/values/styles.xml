<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- Main Activity TextView Style for both portrait and landscape view
            Some elements have been overridden in the both activity xml files.
            Note originally i used larger margins on the landscape view,
            but have subsequently removed them.-->
    <style name="MainTextViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:clickable">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:text">""</item>
        <item name="android:textColor">@color/colorTextWhite</item>
    </style>
    <!-- Message View Activity EditText Style for both portrait and landscape view
        Some elements have been overridden in the both activity xml files-->
    <style name="MsgViewEdtTextStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <item name="android:layout_alignParentStart">true</item>
        <item name="android:textColor">@color/colorTextBlack</item>
    </style>

    <!--Common Button styles-->
    <style name="GeneralButtonStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">16sp</item>
        <!--button text string. Linked to the String.xml file using @ symbol-->
        <item name="android:text">@string/str_Send</item>

    </style>

</resources>
